{
  "sha": "a1b2c3d4e5f6789012345678901234567890abcd",
  "html_url": "https://github.com/urllib3/urllib3/commit/a1b2c3d4e5f6789012345678901234567890abcd",
  "parents": [
    {
      "sha": "0000000000000000000000000000000000000001"
    }
  ],
  "files": [
    {
      "filename": "src/urllib3/response.py",
      "status": "modified",
      "patch": "@@ -120,6 +120,10 @@ class HTTPResponse(io.IOBase):\n     def drain_conn(self):\n         \"\"\"Release the connection back to the pool.\"\"\"\n+        if self._fp is None:\n+            return\n         try:\n             self.read(decode_content=False)\n         except (HTTPError, OSError, BaseSSLError):\n             pass\n"
    },
    {
      "filename": "test/test_response.py",
      "status": "modified",
      "patch": "@@ -45,3 +45,12 @@ def test_drain_conn_closes_body():\n     assert response.closed\n+\n+def test_drain_conn_noop_when_fp_missing():\n+    response = HTTPResponse(None)\n+    response.drain_conn()\n+    assert response.closed is False\n"
    }
  ]
}
