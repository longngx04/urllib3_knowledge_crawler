# C-008 — Phase 7 patch and regression-test enrichment

status: done
deps: C-007
implements: FR33, FR34, FR35, FR36

## Scope

Build the patch enrichment vertical slice: fetch GitHub commit metadata through the
Phase 2 retrieval client, extract changed files/symbols/guards/regression tests from
unified diffs, normalize provenance-backed `PatchRecord` objects with repository
ownership checks and unresolved-ref reporting, and atomically export deterministic
`patches.jsonl`. Do not add security-semantic extraction, KB documents, NVD, or new
CLI pipeline commands.

## Allowed files

- `.agents/implementation_plan.md`
- `README.md`
- `cards/C-008.md`
- `crawler/clients/github.py`
- `crawler/extractors/patch_diff.py`
- `crawler/normalizers/patches.py`
- `crawler/exporters/jsonl.py`
- `docs/patch_enrichment.md`
- `flow/03-prd.md`
- `flow/05-contract.md`
- `tests/fixtures/github_commit_version_api.json`
- `tests/fixtures/github_commit_version_api_config.json`
- `tests/fixtures/github_commit_version_api_dataflow.json`
- `tests/test_github.py`
- `tests/test_patch_diff.py`
- `tests/test_patches.py`

## Verify (run these before calling the card done)

- [x] Run offline GitHub client tests for `fetch_commit` URL construction, SHA
      validation, and explicit rejection of invalid owner/repo/non-200/non-JSON
      responses.
- [x] Run fixture patch-diff tests covering changed files, Python symbols, added
      guards, and regression-test path detection for three vulnerability classes.
- [x] Run fixture patch normalizer tests covering repository ownership checks,
      unresolved refs, no invented fixed versions, at least three `PatchRecord`
      outputs, and deterministic `patches.jsonl` export.
- [x] Run `pytest --cov=crawler`, `ruff check .`, `ruff format .`, and
      `mypy crawler`; expect exit `0`.
- [x] Scan staged content for credentials and generated raw output; expect no secret
      value or crawl artifact staged.

## Done-evidence (world-state proof, named BEFORE building)

From the installed checkout, offline tests prove that three fixture commits normalize
to provenance-backed patch records with extracted diff evidence, repository ownership is
enforced, unresolved patch references are reported, fixed versions are not invented,
and `patches.jsonl` export is byte-deterministic. Paste the actual test count,
coverage, and quality tool output below.

## Evidence (paste the actual proof here when done)

Verified from the project checkout with Python 3.12.3:

```text
$ pytest --cov=crawler --cov-report=term-missing
collected 188 items
188 passed in 2.03s
TOTAL 2073 statements, 169 missed, 92% coverage

New Phase 7 modules:
  crawler/clients/github.py (fetch_commit)     61 stmts, 1 missed, 98% coverage
  crawler/extractors/patch_diff.py             73 stmts, 4 missed, 95% coverage
  crawler/normalizers/patches.py               87 stmts, 10 missed, 89% coverage
  crawler/exporters/jsonl.py (patch export)    64 stmts, 7 missed, 89% coverage

New Phase 7 tests: 24 (6 GitHub fetch_commit, 9 patch diff, 9 patches)

$ ruff check .
All checks passed!

$ ruff format .
68 files left unchanged

$ mypy crawler
Success: no issues found in 30 source files

secret_matches=0
```
