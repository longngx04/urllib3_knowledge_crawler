# C-014 — Phase 13 report and operator README

status: done
deps: C-013
implements: FR54, FR55

## Scope

Publish the engineering crawl report (why urllib3, how crawling works, what was
produced) and a detailed operator README covering install, offline/live run, query,
outputs, and safety. Do not change crawler runtime behavior except documentation and
plan checkboxes.

## Allowed files

- `.agents/implementation_plan.md`
- `README.md`
- `cards/C-014.md`
- `reports/urllib3_crawl_report.md`
- `flow/03-prd.md`
- `flow/05-contract.md`

## Verify

- [x] Report contains the 18 required sections from the implementation plan.
- [x] README documents install, offline `run`, stage commands, query, outputs, and safety.
- [x] Report statistics come from a real offline `run` / `stats.json` (or are labeled).
- [x] `ruff check .`, `ruff format --check .`, `mypy crawler`, `pytest -q` exit 0.

## Evidence

```text
$ python -m crawler run --config configs/urllib3.yaml --output /tmp/urllib3-kb-offline2 \
    --offline --fixture-dir tests/fixtures/pipeline
run complete: 7 versions, 1 advisories, 1 patterns

$ pytest -q
238 passed

report: reports/urllib3_crawl_report.md
readme: README.md
```
