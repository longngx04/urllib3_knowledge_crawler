# C-002 — Phase 1 deterministic data-contract layer

status: done
deps: C-001
implements: FR8, FR9, FR10, FR11, FR12

## Scope

Build the single Phase 1 contract outcome: strict typed records and shared domain
shapes, deterministic stable identifiers, generated checked-in JSON Schemas, explicit
serialization/compatibility decisions, and offline tests proving that the Python and
JSON contracts agree. Do not implement source clients, crawling, advisory alias or
version-range resolution, enrichment, validation commands, exports, or real security
facts.

## Allowed files

- `.agents/implementation_plan.md`
- `README.md`
- `docs/data_contracts.md`
- `crawler/models.py`
- `crawler/exporters/schemas.py`
- `crawler/utils/hashing.py`
- `schemas/**`
- `tests/**`
- `cards/C-002.md`

## Verify (run these before calling the card done)

- [x] Import all six public records and construct representative examples; expect
      strict validation and JSON-compatible serialization.
- [x] Run a public API example that serializes each record and validates it against
      its checked-in schema; expect six successful validations.
- [x] Export schemas to a temporary directory and compare them byte-for-byte with
      `schemas/`; expect no drift.
- [x] Call `stable_record_id` with reordered mapping and set inputs; expect exactly
      one identical `<type>:<sha256>` value.
- [x] Run `pytest`; expect all tests to pass offline.
- [x] Run `ruff check .`, `ruff format --check .`, and `mypy crawler`; expect exit `0`.
- [x] Scan tracked/staged content for credentials and inspect Git status; expect no
      secret value or generated crawl output staged.

## Done-evidence (world-state proof, named BEFORE building)

From a clean installed checkout, a standalone Python process can import the six public
record classes, instantiate one representative record of each type, serialize each to
JSON, and validate all six payloads with the checked-in Draft 2020-12 schemas. The same
process prints one stable record ID for semantically identical reordered identity input
and reports six validated record types. Paste the actual command output and interpreter
path below.

## Evidence (paste the actual proof here when done)

Verified from the clean Python 3.12 environment created at
`/tmp/tmp.wsuKxJ3wre/venv` after installing the project and its development extra:

```text
python=/tmp/tmp.wsuKxJ3wre/venv/bin/python
stable_id=advisory:321ac5844e023c688280ff6af4880ebdf3e1f8248d2290d3c6320d7b97a62a30
validated_count=6
validated_types=advisory,kb_document,patch,provenance,security_pattern,version
clean_install_import=ok
```

The standalone consumer constructed all six public records, serialized them with
Pydantic, validated them with their checked-in Draft 2020-12 schemas, and confirmed
that reordered mapping/set identity input produced the same stable ID.

```text
$ pytest --cov=crawler --cov-report=term-missing
collected 25 items
25 passed in 0.83s
TOTAL 342 statements, 13 missed, 96% coverage

$ ruff check .
All checks passed!

$ ruff format --check .
24 files already formatted

$ mypy crawler
Success: no issues found in 13 source files

schema_drift=none
secret_matches=0
```
