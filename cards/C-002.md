# C-002 — Phase 1 deterministic data-contract layer

status: todo
deps: C-001
implements: FR8, FR9, FR10, FR11, FR12

## Scope

Build the single Phase 1 contract outcome: strict typed records and shared domain
shapes, deterministic stable identifiers, generated checked-in JSON Schemas, explicit
serialization/compatibility decisions, and offline tests proving that the Python and
JSON contracts agree. Do not implement source clients, crawling, advisory alias or
version-range resolution, enrichment, validation commands, exports, or real security
facts.

## Allowed files

- `.agents/implementation_plan.md`
- `README.md`
- `docs/data_contracts.md`
- `crawler/models.py`
- `crawler/exporters/schemas.py`
- `crawler/utils/hashing.py`
- `schemas/**`
- `tests/test_models.py`
- `tests/test_schemas.py`
- `cards/C-002.md`

## Verify (run these before calling the card done)

- [ ] Import all six public records and construct representative examples; expect
      strict validation and JSON-compatible serialization.
- [ ] Run a public API example that serializes each record and validates it against
      its checked-in schema; expect six successful validations.
- [ ] Export schemas to a temporary directory and compare them byte-for-byte with
      `schemas/`; expect no drift.
- [ ] Call `stable_record_id` with reordered mapping and set inputs; expect exactly
      one identical `<type>:<sha256>` value.
- [ ] Run `pytest`; expect all tests to pass offline.
- [ ] Run `ruff check .`, `ruff format --check .`, and `mypy crawler`; expect exit `0`.
- [ ] Scan tracked/staged content for credentials and inspect Git status; expect no
      secret value or generated crawl output staged.

## Done-evidence (world-state proof, named BEFORE building)

From a clean installed checkout, a standalone Python process can import the six public
record classes, instantiate one representative record of each type, serialize each to
JSON, and validate all six payloads with the checked-in Draft 2020-12 schemas. The same
process prints one stable record ID for semantically identical reordered identity input
and reports six validated record types. Paste the actual command output and interpreter
path below.

## Evidence (paste the actual proof here when done)

(empty until done)
