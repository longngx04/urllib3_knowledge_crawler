# C-003 — Phase 2 secure retrieval and raw-cache layer

status: done
deps: C-002
implements: FR13, FR14, FR15, FR16

## Scope

Build the single reusable Phase 2 retrieval boundary: typed crawl settings, safe
request identity, synchronous bounded HTTP retrieval, transient/rate-limit retry,
GitHub-token scoping, SHA-256-verified atomic raw storage, deterministic cache replay,
and offline tests. Do not add source-specific clients, live crawl commands, parsing,
normalization, or real upstream data.

## Allowed files

- `.agents/implementation_plan.md`
- `README.md`
- `configs/urllib3.yaml`
- `crawler/config.py`
- `crawler/utils/cache.py`
- `crawler/utils/http.py`
- `crawler/utils/retry.py`
- `docs/retrieval.md`
- `flow/05-contract.md`
- `tests/**`
- `cards/C-003.md`

## Verify (run these before calling the card done)

- [x] Load `configs/urllib3.yaml` through the public typed config loader; expect all
      timeout/retry/cache/size values to match the contract.
- [x] Run offline tests covering cache miss/hit, offline replay, atomic metadata/body
      persistence, hash corruption, and cache-disabled behavior.
- [x] Run offline tests covering timeout/connection and transient-status retries,
      bounded `Retry-After`, GitHub rate-limit reset, and retry exhaustion.
- [x] Run security tests proving GitHub auth is host-scoped, sensitive caller headers
      and URL credentials are rejected, persisted metadata/logs contain no token, and
      declared/streamed oversized responses fail without storage.
- [x] Run a standalone installed-library example whose first mocked request stores raw
      evidence and whose second request succeeds from cache with a transport that would
      fail if contacted; print cache key, SHA-256, call count, and cache state.
- [x] Run `pytest --cov=crawler`, `ruff check .`, `ruff format --check .`, and
      `mypy crawler`; expect exit `0`.
- [x] Scan staged content for credentials and generated raw output; expect no secret
      value or generated crawl artifact staged.

## Done-evidence (world-state proof, named BEFORE building)

From a clean installed Python 3.12 environment, a standalone consumer uses the public
retrieval API with an HTTPX mock transport. The first fetch returns synthetic bytes and
writes verified raw body/metadata below a temporary root; a second independently
constructed client returns identical bytes from cache while its fail-if-called transport
is never invoked. The output reports one network call, `first.from_cache=false`,
`second.from_cache=true`, identical body SHA-256/cache keys, and no token in persisted
files. Paste the actual interpreter path, output, and raw metadata shape below.

## Evidence (paste the actual proof here when done)

Verified from a clean Python 3.12 environment at
`/tmp/tmp.Z7TVWHa9Ev/venv` after installing the project and development extra. The
standalone consumer used only public Phase 2 APIs and HTTPX mock transports:

```text
python=/tmp/tmp.Z7TVWHa9Ev/venv/bin/python
network_calls=1
first_from_cache=false
second_from_cache=true
cache_key=aa7f3c5d2f0f7216907db582284593672313d721ab246970e2d77ad8069fad2a
body_sha256=ffbc2dfc402782325da71132100e74ff511d1585dd80e4ea196ed4bcace3fef2
metadata_request_keys=body_sha256,method,url
metadata_response_keys=body_sha256,body_size,content_type,headers,retrieved_at,status_code
persisted_token=false
```

The second client used a fail-if-called transport, so the successful second fetch proves
offline replay from verified raw storage rather than another mocked request.

```text
$ pytest --cov=crawler --cov-report=term-missing
collected 69 items
69 passed in 1.04s
TOTAL 760 statements, 49 missed, 94% coverage

$ ruff check .
All checks passed!

$ ruff format --check .
34 files already formatted

$ mypy crawler
Success: no issues found in 17 source files

schema_drift=none
secret_matches=0
```
