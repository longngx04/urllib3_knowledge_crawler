# C-001 — Phase 0 installable CLI bootstrap

status: todo
deps: none
implements: FR1, FR2, FR3, FR4, FR5, FR6, FR7

## Scope

Build the single Phase 0 outcome defined by the contract: an installable, typed Python package with the planned repository boundaries, safe urllib3 configuration and credential template, a minimal help/version CLI, offline bootstrap tests, quality-tool configuration, repository hygiene, and reproducible contributor documentation. Do not implement domain models, schemas, network clients, crawling, normalization, enrichment, validation, statistics, or query behavior.

## Allowed files

- `.agents/implementation_plan.md`
- `.env.example`
- `.gitignore`
- `README.md`
- `pyproject.toml`
- `configs/**`
- `crawler/**`
- `schemas/**`
- `tests/**`
- `data/**`
- `cards/C-001.md`

## Verify (run these before calling the card done)

- [ ] Create a clean Python 3.12 `.venv` and install `.[dev]` successfully.
- [ ] Run `python -m crawler --help`; expect exit `0` and the documented purpose/options.
- [ ] Run `python -m crawler --version`; expect exit `0` and exactly `urllib3-knowledge-crawler 0.1.0`.
- [ ] Run `pytest`; expect exit `0` with all bootstrap tests passing offline.
- [ ] Run `ruff check .` and `ruff format --check .`; expect exit `0`.
- [ ] Run `mypy crawler`; expect exit `0` with no issues.
- [ ] Scan tracked/staged content for credentials and inspect Git status; expect no secret value or generated crawl output staged.

## Done-evidence (world-state proof, named BEFORE building)

A fresh Python 3.12 virtual environment reports installed distribution `urllib3-knowledge-crawler==0.1.0`; invoking the installed module and `urllib3-kb` console script returns the contract-defined help/version text and exit code `0`. Paste the actual install metadata, executable path, command output, and exit codes below.

## Evidence (paste the actual proof here when done)

(empty until done)
