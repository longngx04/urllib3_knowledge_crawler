# C-001 — Phase 0 installable CLI bootstrap

status: done
deps: none
implements: FR1, FR2, FR3, FR4, FR5, FR6, FR7

## Scope

Build the single Phase 0 outcome defined by the contract: an installable, typed Python package with the planned repository boundaries, safe urllib3 configuration and credential template, a minimal help/version CLI, offline bootstrap tests, quality-tool configuration, repository hygiene, and reproducible contributor documentation. Do not implement domain models, schemas, network clients, crawling, normalization, enrichment, validation, statistics, or query behavior.

## Allowed files

- `.agents/implementation_plan.md`
- `.env.example`
- `.gitignore`
- `flow/.lock` (untrack generated runtime state only)
- `README.md`
- `pyproject.toml`
- `configs/**`
- `crawler/**`
- `schemas/**`
- `tests/**`
- `data/**`
- `cards/C-001.md`

## Verify (run these before calling the card done)

- [x] Create a clean Python 3.12 `.venv` and install `.[dev]` successfully.
- [x] Run `python -m crawler --help`; expect exit `0` and the documented purpose/options.
- [x] Run `python -m crawler --version`; expect exit `0` and exactly `urllib3-knowledge-crawler 0.1.0`.
- [x] Run `pytest`; expect exit `0` with all bootstrap tests passing offline.
- [x] Run `ruff check .` and `ruff format --check .`; expect exit `0`.
- [x] Run `mypy crawler`; expect exit `0` with no issues.
- [x] Scan tracked/staged content for credentials and inspect Git status; expect no secret value or generated crawl output staged.

## Done-evidence (world-state proof, named BEFORE building)

A fresh Python 3.12 virtual environment reports installed distribution `urllib3-knowledge-crawler==0.1.0`; invoking the installed module and `urllib3-kb` console script returns the contract-defined help/version text and exit code `0`. Paste the actual install metadata, executable path, command output, and exit codes below.

## Evidence (paste the actual proof here when done)

Verified independently in a fresh Python 3.12.3 virtual environment at `/tmp/tmp.GELRu8Bpdh/venv`:

```text
$ python -m pip show urllib3-knowledge-crawler
Name: urllib3-knowledge-crawler
Version: 0.1.0
Location: /tmp/tmp.GELRu8Bpdh/venv/lib/python3.12/site-packages
Editable project location: /home/longngx04/VinSOC/urllib3_knowledge_crawler
```

The installed console entry point exists and is executable at
`/tmp/tmp.GELRu8Bpdh/venv/bin/urllib3-kb`.

```text
$ python -m crawler --help
Usage: python -m crawler [OPTIONS] COMMAND [ARGS]...
Build version-aware urllib3 security knowledge for AI-assisted SAST.
--version  Show the application version and exit.
--help     Show this message and exit.
exit=0

$ python -m crawler --version
urllib3-knowledge-crawler 0.1.0
exit=0

$ urllib3-kb --version
urllib3-knowledge-crawler 0.1.0
exit=0
```

```text
$ pytest
collected 9 items
tests/test_bootstrap.py ...
tests/test_cli.py ......
9 passed in 0.15s

$ ruff check .
All checks passed!

$ ruff format --check .
16 files already formatted

$ mypy crawler
Success: no issues found in 10 source files
```

Credential-pattern files: `0`. Non-empty `GITHUB_TOKEN`/`NVD_API_KEY` assignment files: `0`.
Git ignore checks confirmed `.env`, virtual environments, generated data, `flow/.lock`, and
`cards/.inflight` are ignored while `.env.example` and data `.gitkeep` markers remain trackable.
