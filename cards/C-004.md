# C-004 — Phase 3 authoritative PyPI version inventory

status: todo
deps: C-003
implements: FR17, FR18, FR19, FR20

## Scope

Build one PyPI-to-JSONL vertical slice for the configured package: fetch bounded project
JSON through the Phase 2 retrieval client/raw store, normalize every parsable release
and distribution into provenance-backed Phase 1 records, report unparsable release
keys, validate project/artifact/version invariants, expose inventory statistics, and
atomically export deterministic `versions.jsonl`. Do not add GitHub correlation,
advisory/range logic, new CLI pipeline commands, or checked-in generated crawl data.

## Allowed files

- `.agents/implementation_plan.md`
- `README.md`
- `cards/C-004.md`
- `crawler/clients/pypi.py`
- `crawler/exporters/jsonl.py`
- `crawler/models.py`
- `crawler/normalizers/versions.py`
- `crawler/validators/versions.py`
- `docs/data_contracts.md`
- `docs/pypi_versions.md`
- `flow/02-scope.md`
- `flow/03-prd.md`
- `flow/05-contract.md`
- `schemas/version.schema.json`
- `tests/fixtures/pypi_project.json`
- `tests/test_jsonl.py`
- `tests/test_models.py`
- `tests/test_pypi.py`
- `tests/test_versions.py`

## Verify (run these before calling the card done)

- [ ] Run offline client tests proving canonical PyPI URL construction, shared raw-cache
      use, and explicit rejection of invalid names/non-200/non-JSON responses.
- [ ] Run fixture normalizer/validator tests covering PEP 440 sorting, prereleases,
      yanked releases/reasons, artifact metadata, empty releases, unparsable keys,
      wrong projects, malformed metadata, and normalized-version collisions.
- [ ] Export the same fixture inventory twice; require byte-identical UTF-8 JSONL,
      independent schema-valid lines, atomic fixed-path output, and identical SHA-256.
- [ ] Run one live standalone installed-library crawl of public PyPI into an ignored
      data root; require a preserved raw body/metadata pair, generated
      `versions.jsonl`, PEP 440 ordering, zero validation failures, and printed stats.
- [ ] Run `pytest --cov=crawler`, `ruff check .`, `ruff format --check .`, and
      `mypy crawler`; expect exit `0`.
- [ ] Scan staged content for credentials and generated raw/normalized data; expect no
      secret value or crawl artifact staged.

## Done-evidence (world-state proof, named BEFORE building)

From a clean installed Python 3.12 environment, a standalone consumer uses only the
public Phase 2/3 APIs to fetch the live public urllib3 PyPI JSON into a temporary
`data/raw/pypi` store, normalize and validate it, and atomically write
`data/normalized/versions.jsonl`. Paste the actual interpreter, source URL, raw body
SHA/path, output SHA/path, record/artifact/prerelease/yanked/unparsable totals, first and
last normalized versions, and a second cached fetch proving zero additional transport
dependency. No generated crawl data is committed.

## Evidence (paste the actual proof here when done)

(empty until done)
