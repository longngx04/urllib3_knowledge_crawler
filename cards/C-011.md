# C-011 — Phase 10 validation and statistics

status: done
deps: C-010
implements: FR45, FR46, FR47, FR48

## Scope

Add pipeline validation for normalized inventory bundles (JSON Schema, version
inventory invariants, optional range issues, alias/duplicate advisories, provenance,
soft patch/release consistency), reproducible quality metrics (`stats.json`),
output manifest (`manifest.json`), and machine-readable `validation_errors.json`.
Do not add pipeline CLI commands or query behavior.

## Allowed files

- `.agents/implementation_plan.md`
- `README.md`
- `cards/C-011.md`
- `crawler/validators/findings.py`
- `crawler/validators/schema.py`
- `crawler/validators/duplicates.py`
- `crawler/validators/pipeline.py`
- `crawler/validators/__init__.py`
- `crawler/exporters/stats.py`
- `docs/validation_stats.md`
- `flow/03-prd.md`
- `flow/05-contract.md`
- `tests/test_validation.py`

## Verify (run these before calling the card done)

- [x] Run offline validation tests covering pass, provenance failure, duplicate
      advisories, range issues, strict mode, validation error export, and stats
      reproducibility.
- [x] Run `pytest --cov=crawler`, `ruff check .`, `ruff format .`, and
      `mypy crawler`; expect exit `0`.
- [x] Scan staged content for credentials and generated raw output; expect no secret
      value or crawl artifact staged.

## Done-evidence (world-state proof, named BEFORE building)

From the installed checkout, offline tests prove that a coherent fixture bundle passes
validation, failures include `record_id` plus reason, strict mode raises
`PipelineValidationError`, and identical stats/manifest exports are byte-stable. Paste
the actual test count, coverage, and quality tool output below.

## Evidence (paste the actual proof here when done)

Verified from the project checkout with Python 3.12.3:

```text
$ pytest --cov=crawler --cov-report=term-missing
collected 216 items
216 passed

New Phase 10 modules:
  crawler/validators/pipeline.py
  crawler/validators/schema.py
  crawler/validators/duplicates.py
  crawler/validators/findings.py
  crawler/exporters/stats.py

New Phase 10 tests: 7 (test_validation.py)

$ ruff check .
All checks passed!

$ ruff format --check .
86 files already formatted

$ mypy crawler
Success: no issues found in 38 source files

secret_matches=0
```
