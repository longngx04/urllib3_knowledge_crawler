# C-007 — Phase 6 version-range resolution

status: done
deps: C-006
implements: FR29, FR30, FR31, FR32

## Scope

Build the version-range resolution vertical slice: evaluate OSV-style
introduced/fixed/last_affected/limit events and PEP 440 specifier expressions
against the Phase 3 PyPI inventory, write deterministic `VersionRange.resolved`
and advisory `affected_versions` lists, preserve raw ranges, verify fixed versions
exist in the inventory without inventing them, report invalid/contradictory/
unresolvable ranges, and expose coverage metrics. Do not add patch enrichment,
security-semantic extraction, NVD, or new CLI pipeline commands.

## Allowed files

- `.agents/implementation_plan.md`
- `README.md`
- `cards/C-007.md`
- `crawler/resolvers/ranges.py`
- `docs/range_resolution.md`
- `flow/02-scope.md`
- `flow/03-prd.md`
- `flow/05-contract.md`
- `tests/test_ranges.py`

## Verify (run these before calling the card done)

- [x] Run offline tests proving specifier inclusion/exclusion, open-ended lower and
      upper bounds, prerelease ordering, yanked inventory inclusion, invalid ranges,
      multiple disjoint ranges, raw preservation, missing fixed versions, contradictory
      overlaps, unresolvable advisories, coverage metrics, and deterministic repeats.
- [x] Run `pytest --cov=crawler`, `ruff check .`, `ruff format --check .`, and
      `mypy crawler`; expect exit `0`.
- [x] Scan staged content for credentials and generated raw output; expect no secret
      value or crawl artifact staged.

## Done-evidence (world-state proof, named BEFORE building)

From the installed checkout, offline tests prove that fixture advisories with
OSV events and PEP 440 specifier ranges resolve to deterministic affected-version
lists against a PyPI inventory, invalid and contradictory ranges are reported, missing
fixed versions are surfaced without invention, and coverage metrics are available.
Paste the actual test count, coverage, and quality tool output below.

## Evidence (paste the actual proof here when done)

Verified from the project checkout with Python 3.12.3:

```text
$ pytest --cov=crawler --cov-report=term-missing
collected 164 items
164 passed in 1.85s
TOTAL 1884 statements, 154 missed, 92% coverage

New Phase 6 module:
  crawler/resolvers/ranges.py  192 stmts, 13 missed, 93% coverage

New Phase 6 tests: 12 offline range-resolution cases

$ ruff check .
All checks passed!

$ ruff format --check .
62 files already formatted

$ mypy crawler
Success: no issues found in 28 source files

secret_matches=0
```
