# Copied to .env locally (never commit .env).
# The CLI auto-loads these allowlisted keys from .env when present.
# Shell exports still override values from this file.

# Optional GitHub API token. Recommended for live crawls (rate limits).
GITHUB_TOKEN=

# Optional NVD API key. Reserved for future enrichment; unused by default path.
NVD_API_KEY=
